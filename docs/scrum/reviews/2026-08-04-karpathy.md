# Karpathy-Review — Tagesdiff 04.08.2026 (`80b52ae..cead9f9`)

**Reviewer:** karpathy-reviewer (frischer Kontext, Cherny-Muster) ·
**Datum:** 04.08.2026 · **Prüfgegenstand:** die drei Commits `e3fb887`,
`5294094`, `cead9f9` — CI-Einrichtung, drei Prozessregeln, Kegel-Rückbau,
neutrale README-Bilder samt Läufer `readmeshots`, SPEC-2.4-Ergänzung,
Workflow-Analyse als Bericht. Dieser Lauf schließt den offenen Punkt aus
`2026-08-04-ci/BERICHT.md` §6.

**Task:** Die Arbeit des 04.08.2026 gegen die vier Prinzipien halten;
Messbehauptungen stichprobenartig nachrechnen; Orphans nach dem Kegel-Rückbau
suchen; Verankerungsorte prüfen.

**Gesamt-Verdict: warn** — keine `fail`-Befunde, drei `warn`-Befunde mit
Einzeiler-Fixes.

---

## Prinzip 1 — Think Before Acting

**Verdict: ok**

**Beobachtungen:**

- Die pipefail-Selbstberichtigung (`2026-08-04-ci/BERICHT.md:79–95`) ist das
  Gegenteil einer stillen Korrektur: Der echte Lauf widerlegte die eigene
  Begründung, beide Kommentare wurden berichtigt, und der Bericht erklärt,
  warum die Wachprobe den Fehler nicht hätte finden können.
- Der Kürzel-Bericht flaggt selbst den einen SPEC-Halbsatz, der über das
  Gebaute hinausgeht („Die Meldung nennt das betroffene Kürzel",
  `2026-08-04-kuerzel-recorder.md:198–205`), und legt die Entscheidung dem PO
  vor, statt sie zu treffen. Nachgeprüft: Für das einzige gebaute Kürzel
  (`Meta+N`) stimmt der Satz heute; er bindet erst die M4-Umsetzung.
- Annahmen tragen durchgehend Quellen und Befehle (CI-Paketliste aus
  `pacman -Qoq`, Farbwerte aus der Palette-Messung vom 01.08.,
  `readmeshots.cpp:65–73`).

## Prinzip 2 — Simplicity First

**Verdict: ok**

**Beobachtungen:**

- `ci.yml` ist minimal: ein Job, sechs Schritte, keine Matrix, kein Cache —
  und der Bericht begründet den Verzicht auf den Cache mit der Messung
  (40 s von 1 min 45 s, `BERICHT.md:120–124`).
- `readmeshots.cpp` ist ein einzelner Läufer für beide Bilder statt zweier
  Eingriffe in abgenommene Belegreihen (`2026-08-04-neutrale-bilder/bericht.md`
  §2) — die einfachere der geprüften Alternativen.
- Der Kegel-Rückbau entfernt netto über 1.000 Zeilen; der verbleibende
  Gedächtnisvermerk (`PROZESS.md:122–133`) ist die minimale Form, die einen
  Wiedervorschlag ohne Aufwandszahl verhindert.
- *Beobachtung ohne Beanstandung:* Der Tag fügt `docs/scrum/` rund 1.350
  Zeilen Berichte hinzu, am selben Tag, an dem die Pflicht-/Kürteil-Regel den
  Doku-Wuchs begrenzen soll. Die Berichte sind jedoch Belege im Sinne von B7
  („Gekürzt wird die Prosa, nie der Beleg"), kein Selbstzweck.

## Prinzip 3 — Surgical Changes

**Verdict: warn**

**Beobachtungen:**

- **[W1] Die Bildläufer-Aufzählung ist nach dem vierten Läufer unvollständig.**
  `CLAUDE.md` (Prüfhaltung: „Die Bildläufer (`editshots`, `libraryshots`,
  `searchshots`) sind `EXCLUDE_FROM_ALL`") und `ci.yml:12` zählen drei Läufer
  auf; seit `cead9f9` existiert mit `readmeshots` ein vierter mit derselben
  Falle (`tests/CMakeLists.txt:86`). Beide Listen lesen sich abschließend, und
  `CLAUDE.md` wird automatisch geladen — wer die README-Bilder mit einem
  veralteten Läufer erneuert, wiederholt den Sprint-5-Vorfall dort, wo das
  Repo öffentlich hinzeigt. Die Workflow-Analyse benennt die Falle
  (`2026-08-04-workflow-optimierungen.md`, V1: „`readmeshots` ist heute als
  vierter Läufer dazugekommen und trägt dieselbe Falle"), aber nur als Beleg
  eines unentschiedenen Vorschlags — der geltende Text bleibt unvollständig.
  *Schlussfolgerung:* Orphan der eigenen Änderung im Sinne von Karpathy 3;
  aufzuräumen unabhängig davon, wie der Kunde über V1 entscheidet.
- **[W2] Innerer Widerspruch in `sprint-06.md` §13.** Die Einleitung sagt
  „Drei der vier Punkte sind **heute umgesetzt**, einer **liegt dem Kunden
  vor**" (`sprint-06.md:956`); die vom Rückbau geänderte Überschrift §13.4
  sagt „**Entschieden** — der Schätzkegel wird entfernt". Der Rückbau hat
  §13.4 angefasst (der Abschnitt ist an dieser Stelle also kein unantastbares
  Historien-Dokument), die Einleitung aber nicht nachgezogen.
  *Schlussfolgerung:* kein Verstoß gegen die Protokoll-Schonung, sondern eine
  halbe Nachführung.
- **Geprüft und nicht beanstandet:** Der Diff bleibt im beauftragten Rahmen.
  Kein V1–V4-Vorschlag der Workflow-Analyse ist stillschweigend umgesetzt
  (nachgeprüft: `readmeshots` trägt `EXCLUDE_FROM_ALL` wie zuvor alle Läufer;
  keine Prüfsummen-Wache in `PROZESS.md`; keine Linter in `ci.yml`;
  `denkzettel-dev.md` unverändert). Der appstreamtest-Nebenbefund
  (`sprint-06.md` §13.5) ist gemeldet, nicht geheilt. Die Sprint-Protokolle
  tragen den Kegel weiter — absichtsgemäß.

## Prinzip 4 — Goal-Driven Execution

**Verdict: warn**

**Beobachtungen:**

- **[W3] `PROZESS.md:3` trägt „Stand: 2026-08-02" nach drei Änderungen am
  04.08.** Der Rückbau-Bericht meldet das als Altbefund und legt die
  Entscheidung dem PO vor (`sprint-06-kegel-rueckbau.md` §3) — am Tagesende
  ist weder fortgeschrieben noch gestrichen noch eine Entscheidung
  dokumentiert. *Schlussfolgerung:* genau die Bauart „still falsches
  Standdatum", die das Projekt beim Kegel-Feld `stand` selbst als Fehlerklasse
  beschrieben hat; ein gemeldeter, aber unadressierter Loose End.
- **Geprüft und nicht beanstandet — die Messbehauptungen tragen:**
  - **Zeilenzahlen** (`PROZESS.md`, Pflicht-/Kürteil; `sprint-06.md` §13):
    nachgerechnet am Messstand `80b52ae` — `docs/scrum/**/*.md` = **13.659**
    Zeilen, `src/` = **3.894** Zeilen, beide exakt. (Nach den Tagescommits
    sind es 15.011; die Angabe ist als datierte Messung gekennzeichnet.)
  - **Determinismus-Zusage `readmeshots`** (`README.md:116–117`): zwei frische
    Läufe ausgeführt — bytegleich untereinander **und** identisch mit den
    committeten `docs/bilder/*.png`. Strukturell gestützt durch fixe
    Referenzzeit (`readmeshots.cpp:42–45`, `:192`), temporäre Konfiguration
    und temporären Store.
  - **CI-Bericht:** „7/7 grün" stimmt (`ctest -N`: 7 Tests); die Wachprobe
    liegt versioniert bei (`wachprobe.sh`/`.txt`, B7); „drei ihrer sechs
    Punkte" ist mit der DoD-Liste (`PROZESS.md:205–241`) konsistent — DoD 3
    umfasst dort karpathy-Durchgang *und* Bildprüfung, beide Verweise des
    Berichts stimmen.
- **Geprüft und nicht beanstandet — Kegel-Rückbau vollständig:** `git grep`
  über die geltenden Dateien findet als einzigen Resttreffer den beabsichtigten
  Gedächtnisvermerk (`PROZESS.md:122–123`, Vergangenheitsform, benennt den
  ehemaligen Ort). Der README-Kegelabsatz samt totem Bildverweis ist durch den
  CI-Absatz ersetzt; die Abschlussliste endet bei 11, keine geltende Datei
  verweist auf Punkt 12 oder zählt „zwölf Punkte"; der Verwalter-Agent ist
  bereinigt.
- **Geprüft und nicht beanstandet — Verankerungsorte:** Die CI-Regel steht
  dreifach an automatisch gelesenen Orten (`CLAUDE.md`-Push-Absatz — wird
  geladen; `ci.yml`-Kopf — sieht jeder Leser des Workflows; `PROZESS.md`).
  Die Verwalter-Existenzprüfung hängt am SM-geprüften Abschlusspunkt 11 mit
  Exit-Code-Kriterium (`git ls-files`), nicht an einer dritten Ermahnung.
  Pflicht-/Kürteil und Kegel-Vermerk stehen in `PROZESS.md` bei den Regeln,
  die sie ändern; Leser sind SM und PO, deren Einstiege (`scrum-master.md`,
  Projekt-`CLAUDE.md`) auf `PROZESS.md` verweisen.
- **Geprüft und nicht beanstandet — Bilder selbst angesehen** (DoD-3-Geist):
  beide `docs/bilder/*.png` zeigen erfundene Alltagsnotizen ohne
  personenbezogene Inhalte; Umlaute und ß korrekt gerendert („Bücherei",
  „Straßenbahnen") — das echte Plattformthema war geladen.

---

## Konkrete Fix-Vorschläge

1. **[W1]** `readmeshots` in die Läuferliste aufnehmen: `CLAUDE.md`
   (Prüfhaltung, Klammer der Bildläufer) und `.github/workflows/ci.yml:12`
   — je ein Wort.
2. **[W2]** `sprint-06.md:956` nachführen: „Drei … umgesetzt, einer liegt
   dem Kunden vor" → alle vier sind entschieden; ein datierter Halbsatz genügt.
3. **[W3]** `PROZESS.md:3`: Standdatum auf 2026-08-04 fortschreiben **oder**
   die Zeile streichen — die gemeldete PO-Entscheidung treffen und
   dokumentieren, nicht liegen lassen.

## Was gut ist

- Die Selbstberichtigung im CI-Bericht (pipefail) samt Begründung, warum die
  eigene Wachprobe den Fehler nicht gefunden hätte — beibehalten.
- Messungen mit Stand-Kennzeichnung und beiliegendem Befehl: Jede der drei
  stichprobenartig nachgerechneten Zahlen hielt der Nachrechnung stand.
- Konsequente Trennung von „ermitteln" und „umsetzen" in der Workflow-Analyse
  — kein einziger Vorschlag ist der Entscheidung vorausgeeilt.
- Der Läufer `readmeshots` mit fixer Referenzzeit und temporärem Store:
  Determinismus als Bauart, nicht als Behauptung.
